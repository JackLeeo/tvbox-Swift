name: Build TrollStore TVBox

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-14
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          lfs: true
          
      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.2'
          
      - name: Install xcodegen
        run: brew install xcodegen
          
      # ========== 新增：自动下载替换RN插件版NodeMobile框架 ==========
      - name: Replace with RN Plugin NodeMobile Framework
        run: |
          echo "=== 下载RN插件版NodeMobile框架 ==="
          
          # 配置版本号（可随时更新）
          NODEJS_VERSION="18.20.4"
          TARGET_DIR="Libraries/nodejs-mobile"
          
          # 创建目标目录
          mkdir -p $TARGET_DIR
          rm -rf $TARGET_DIR/NodeMobile.xcframework
          
          # 下载npm包并提取xcframework（比npm install更快）
          curl -L "https://registry.npmjs.org/nodejs-mobile-react-native/-/nodejs-mobile-react-native-$NODEJS_VERSION.tgz" -o nodejs-mobile.tgz
          
          # 解压并提取ios目录下的xcframework
          tar -xzf nodejs-mobile.tgz --strip-components=1 package/ios/NodeMobile.xcframework
          
          # 移动到目标目录
          mv NodeMobile.xcframework $TARGET_DIR/
          
          # 清理临时文件
          rm -f nodejs-mobile.tgz
          
          echo "✅ 框架替换完成"
          echo "框架路径: $TARGET_DIR/NodeMobile.xcframework"
          ls -la $TARGET_DIR/NodeMobile.xcframework/
      # ============================================================
          
      - name: Generate Xcode Project
        run: xcodegen

      - name: Detailed NodeMobile Symbol Analysis
        run: |
          echo "=== 详细分析 NodeMobile 框架中的所有符号 ==="
          
          FRAMEWORK_PATH="Libraries/nodejs-mobile/NodeMobile.xcframework/ios-arm64/NodeMobile.framework"
          BINARY_PATH="$FRAMEWORK_PATH/NodeMobile"
          
          if [ -f "$BINARY_PATH" ]; then
            echo "✅ 找到 NodeMobile 二进制文件: $BINARY_PATH"
            
            echo "1. 文件基本信息:"
            file "$BINARY_PATH"
            echo ""
            
            echo "2. 支持的架构:"
            lipo -info "$BINARY_PATH" 2>/dev/null || echo "无法获取架构信息"
            echo ""
            
            echo "3. 查找关键函数:"
            echo "node_start:"
            nm "$BINARY_PATH" 2>/dev/null | grep "_node_start" || echo "未找到"
            echo ""
            echo "node_post_message:"
            nm "$BINARY_PATH" 2>/dev/null | grep "_node_post_message" || echo "未找到"
            echo ""
            echo "node_register_message_callback:"
            nm "$BINARY_PATH" 2>/dev/null | grep "_node_register_message_callback" || echo "未找到"
            
          else
            echo "❌ NodeMobile 二进制文件不存在: $BINARY_PATH"
            exit 1
          fi

      - name: Upload Symbol Analysis Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: node-symbol-analysis
          path: ./symbol-analysis/
          retention-days: 7
          
      - name: Build TVBox
        run: |
          echo "=== 清理构建目录 ==="
          xcodebuild clean -project tvbox.xcodeproj -scheme tvbox -destination 'generic/platform=iOS' || echo "清理完成"
          
          echo "=== 开始构建应用 ==="
          
          set +o pipefail
          xcodebuild build \
            -project tvbox.xcodeproj \
            -scheme tvbox \
            -destination 'generic/platform=iOS' \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            -derivedDataPath ./DerivedData \
            2>&1 | tee build.log
          
          BUILD_EXIT_CODE=${PIPESTATUS[0]}
          
          if [ $BUILD_EXIT_CODE -eq 0 ]; then
            echo "✅ 构建成功"
            
            APP_PATH=$(find ./DerivedData -name "tvbox.app" -type d | head -1)
            if [ -z "$APP_PATH" ]; then
              APP_PATH=$(find ./DerivedData -name "TVBox.app" -type d | head -1)
            fi
            
            if [ -z "$APP_PATH" ]; then
              echo "❌ 未找到.app文件"
              find ./DerivedData -name "*.app" -type d
              exit 1
            fi
            
            echo "APP_PATH: $APP_PATH"
            
            echo "=== 验证.app包结构 ==="
            
            if [ -f "$APP_PATH/TVBox" ] || [ -f "$APP_PATH/tvbox" ]; then
              echo "✅ 可执行文件存在"
            else
              echo "⚠️ 可执行文件不存在"
              find "$APP_PATH" -type f -name "*" | grep -v ".DS_Store"
              
              if [ -f "$APP_PATH/__preview.dylib" ]; then
                mv "$APP_PATH/__preview.dylib" "$APP_PATH/TVBox"
                echo "✅ 已重命名 __preview.dylib 为 TVBox"
              fi
            fi
            
            if [ -f "$APP_PATH/Info.plist" ]; then
              echo "✅ Info.plist存在"
            else
              echo "⚠️ 创建自动修复 Info.plist"
              printf '%s\n' \
                '<?xml version="1.0" encoding="UTF-8"?>' \
                '<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">' \
                '<plist version="1.0">' \
                '<dict>' \
                '    <key>CFBundleDevelopmentRegion</key><string>en</string>' \
                '    <key>CFBundleDisplayName</key><string>TVBox</string>' \
                '    <key>CFBundleExecutable</key><string>TVBox</string>' \
                '    <key>CFBundleIdentifier</key><string>com.tvbox.app</string>' \
                '    <key>CFBundleInfoDictionaryVersion</key><string>6.0</string>' \
                '    <key>CFBundleName</key><string>TVBox</string>' \
                '    <key>CFBundlePackageType</key><string>APPL</string>' \
                '    <key>CFBundleShortVersionString</key><string>1.0.3</string>' \
                '    <key>CFBundleVersion</key><string>1</string>' \
                '    <key>LSRequiresIPhoneOS</key><true/>' \
                '    <key>UIApplicationSupportsIndirectInputEvents</key><true/>' \
                '    <key>UILaunchScreen</key><dict/>' \
                '    <key>UIRequiredDeviceCapabilities</key><array><string>armv7</string></array>' \
                '    <key>UISupportedInterfaceOrientations</key><array><string>UIInterfaceOrientationPortrait</string><string>UIInterfaceOrientationLandscapeLeft</string><string>UIInterfaceOrientationLandscapeRight</string></array>' \
                '    <key>UISupportedInterfaceOrientations~ipad</key><array><string>UIInterfaceOrientationPortrait</string><string>UIInterfaceOrientationPortraitUpsideDown</string><string>UIInterfaceOrientationLandscapeLeft</string><string>UIInterfaceOrientationLandscapeRight</string></array>' \
                '    <key>NSAppTransportSecurity</key><dict><key>NSAllowsArbitraryLoads</key><true/></dict>' \
                '</dict></plist>' \
                > "$APP_PATH/Info.plist"
              echo "✅ Info.plist 创建完成"
            fi
            
            if [ ! -f "$APP_PATH/PkgInfo" ]; then
              echo "APPL????" > "$APP_PATH/PkgInfo"
              echo "✅ 已创建PkgInfo"
            fi
            
            if [ -f "$APP_PATH/Info.plist" ] && [ -f "$APP_PATH/TVBox" ]; then
              /usr/libexec/PlistBuddy -c "Set :CFBundleExecutable TVBox" "$APP_PATH/Info.plist" 2>/dev/null || true
            fi
            
            echo ".app包内容:"
            ls -la "$APP_PATH/"
            
            echo "=== 打包IPA ==="
            mkdir -p ./Build
            TEMP_DIR=$(mktemp -d)
            mkdir -p "$TEMP_DIR/Payload"
            
            cp -r "$APP_PATH" "$TEMP_DIR/Payload/"
            
            cd "$TEMP_DIR"
            zip -r "$GITHUB_WORKSPACE/Build/tvbox-Swift-TrollStore.ipa" Payload
            
            cd "$GITHUB_WORKSPACE"
            rm -rf "$TEMP_DIR"
            
            echo "✅ IPA打包完成: ./Build/tvbox-Swift-TrollStore.ipa"
            ls -lh ./Build/tvbox-Swift-TrollStore.ipa
            
          else
            echo "❌ 构建失败，退出码: $BUILD_EXIT_CODE"
            echo "=== 构建错误摘要 ==="
            
            echo "1. 链接器错误:"
            grep -A5 -B5 "ld:.*not found" build.log | head -20 || echo "无链接器错误"
            
            echo "2. 编译错误:"
            grep -A5 -B5 "error:" build.log | head -20 || echo "无编译错误"
            
            echo "3. 最后50行构建日志:"
            tail -50 build.log
            
            exit 1
          fi
          
      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: tvbox-Swift-TrollStore
          path: ./Build/tvbox-Swift-TrollStore.ipa
          retention-days: 30
          
      - name: Upload Build Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: ./build.log
          retention-days: 7
